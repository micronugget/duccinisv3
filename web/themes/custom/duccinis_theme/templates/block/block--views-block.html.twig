{#
/**
 * @file
 * Theme override for Views block.
 *
 * Attaches the menu_accordion library to enable accordion transformation
 * for product variation menu blocks placed via Layout Builder.
 */
#}

{{ attach_library('duccinis_theme/menu_accordion') }}

{% include 'radix:block' %}
