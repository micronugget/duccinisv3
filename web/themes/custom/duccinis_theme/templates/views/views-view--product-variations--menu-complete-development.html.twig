{#
/**
 * @file
 * Theme override for the Product Variations view, display: Menu complete development.
 *
 * Wraps the grouped output in a single Bootstrap 5 accordion container.
 */
#}

{#
  Keep the view wrapper markup consistent with Radix.
  The accordion markup is handled by the display-specific
  `views-view-unformatted--product-variations--menu-complete-development.html.twig`.
#}

{% include "radix:views-view" %}
